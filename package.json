{
  "name": "blockchain-payload-tools",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build-debug": "browserify src/bpteth.js --s BptEth > dist/bpteth.js",
    "build-min": "browserify src/bpteth.js --s BptEth | uglifyjs -c > dist/bpteth.min.js",
    "build": "npm run build-debug && npm run build-min"
  },
  "author": "Boyan Yankov",
  "license": "Apache-2.0",
  "dependencies": {
    "web3": "^0.19.1"
  },
  "devDependencies": {
    "@babel/core": "^7.4.5",
    "babelify": "^10.0.0",
    "uglify-es": "^3.3.9"
  }
}
